{% extends 'base.html.twig' %}

{% block title %}Accueil - MarokiCars | Location de voitures au Maroc{% endblock %}

{% block body %}
<!-- Hero Section -->
<section class="hero-section py-20 lg:py-32 relative">
    <div class="container mx-auto px-4 relative z-10">
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
            <div class="text-center lg:text-left animate-fade-in">
                <div class="inline-flex items-center space-x-2 bg-white/10 backdrop-blur-sm border border-white/20 rounded-full px-4 py-2 mb-6">
                    <i class="bi bi-star-fill text-accent-400"></i>
                    <span class="text-white text-sm font-medium">Service Premium depuis 2024</span>
                </div>
                
                <h1 class="text-5xl lg:text-7xl font-bold text-white mb-6 leading-tight">
                    Location de voitures<br>
                    <span class="gradient-text-accent">au Maroc</span>
                </h1>
                
                <p class="text-xl text-white/90 mb-8 max-w-xl mx-auto lg:mx-0 leading-relaxed">
                    Réservez votre véhicule en quelques clics et profitez de votre voyage en toute sérénité. 
                    Large choix, prix compétitifs, service 24/7.
                </p>
                
                <div class="flex flex-wrap gap-4 justify-center lg:justify-start mb-12">
                    <a href="{{ path('app_voiture_index') }}" class="btn-primary inline-flex items-center space-x-2">
                        <span>Découvrir nos voitures</span>
                        <i class="bi bi-arrow-right-circle"></i>
                    </a>
                    {% if not app.user %}
                    <a href="{{ path('app_register') }}" class="btn-outline-primary border-2 border-white text-white hover:bg-white hover:text-primary-600 inline-flex items-center space-x-2">
                        <span>S'inscrire gratuitement</span>
                        <i class="bi bi-person-plus"></i>
                    </a>
                    {% endif %}
                </div>
                
                <!-- Stats -->
                <div class="grid grid-cols-3 gap-6 pt-8 border-t border-white/20">
                    <div class="text-center lg:text-left">
                        <div class="text-4xl font-bold gradient-text-accent mb-1">{{ voitures|length }}+</div>
                        <p class="text-white/70 text-sm">Véhicules</p>
                    </div>
                    <div class="text-center">
                        <div class="text-4xl font-bold text-green-400 mb-1">500+</div>
                        <p class="text-white/70 text-sm">Clients satisfaits</p>
                    </div>
                    <div class="text-center lg:text-right">
                        <div class="text-4xl font-bold text-accent-400 mb-1">24/7</div>
                        <p class="text-white/70 text-sm">Support</p>
                    </div>
                </div>
            </div>
            
            <div class="hidden lg:block relative">
                <div class="absolute inset-0 bg-gradient-to-br from-primary-500/30 to-accent-500/30 rounded-3xl blur-3xl transform rotate-6"></div>
                <div class="relative bg-white/10 backdrop-blur-lg rounded-3xl p-12 border border-white/20">
                    <div class="text-center">
                        <i class="bi bi-car-front-fill text-white/20" style="font-size: 12rem;"></i>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Categories Section -->
<section class="py-16 lg:py-24 bg-gradient-to-b from-slate-50 to-white">
    <div class="container mx-auto px-4">
        <div class="text-center mb-12">
            <div class="inline-flex items-center space-x-2 bg-accent-100 text-accent-700 rounded-full px-4 py-2 mb-4">
                <i class="bi bi-tags"></i>
                <span class="text-sm font-semibold">Parcourir par catégorie</span>
            </div>
            <h2 class="text-4xl lg:text-5xl font-bold text-slate-900 mb-4">Choisissez selon vos besoins</h2>
            <p class="text-slate-600 text-lg max-w-2xl mx-auto">
                Trouvez rapidement le véhicule parfait en sélectionnant la catégorie qui correspond à vos besoins.
            </p>
        </div>
        
        <div class="grid grid-cols-2 md:grid-cols-4 gap-6 mb-12">
            <a href="{{ path('app_categorie_index') }}" class="card group hover:shadow-xl transition-all duration-300 text-center p-6">
                <div class="w-16 h-16 bg-blue-100 rounded-2xl flex items-center justify-center mx-auto mb-4 group-hover:scale-110 transition-transform duration-300">
                    <i class="bi bi-people-fill text-blue-600 text-2xl"></i>
                </div>
                <h4 class="font-bold text-slate-900 mb-2">Toutes les catégories</h4>
                <p class="text-slate-600 text-sm">Explorer par type</p>
            </a>
            
            <a href="{{ path('app_voiture_index') }}" class="card group hover:shadow-xl transition-all duration-300 text-center p-6">
                <div class="w-16 h-16 bg-green-100 rounded-2xl flex items-center justify-center mx-auto mb-4 group-hover:scale-110 transition-transform duration-300">
                    <i class="bi bi-car-front text-green-600 text-2xl"></i>
                </div>
                <h4 class="font-bold text-slate-900 mb-2">Toutes les voitures</h4>
                <p class="text-slate-600 text-sm">Voir tout</p>
            </a>
            
            <a href="{{ path('app_voiture_index') }}?statut=disponible" class="card group hover:shadow-xl transition-all duration-300 text-center p-6">
                <div class="w-16 h-16 bg-emerald-100 rounded-2xl flex items-center justify-center mx-auto mb-4 group-hover:scale-110 transition-transform duration-300">
                    <i class="bi bi-check-circle text-emerald-600 text-2xl"></i>
                </div>
                <h4 class="font-bold text-slate-900 mb-2">Disponibles</h4>
                <p class="text-slate-600 text-sm">Maintenant</p>
            </a>
            
            <a href="{{ path('app_voiture_index') }}?promo=1" class="card group hover:shadow-xl transition-all duration-300 text-center p-6">
                <div class="w-16 h-16 bg-red-100 rounded-2xl flex items-center justify-center mx-auto mb-4 group-hover:scale-110 transition-transform duration-300">
                    <i class="bi bi-tag-fill text-red-600 text-2xl"></i>
                </div>
                <h4 class="font-bold text-slate-900 mb-2">Promotions</h4>
                <p class="text-slate-600 text-sm">Meilleurs prix</p>
            </a>
        </div>
    </div>
</section>

<!-- Featured Vehicles Section -->
<section class="py-16 lg:py-24 bg-white">
    <div class="container mx-auto px-4">
        <div class="text-center mb-12">
            <div class="inline-flex items-center space-x-2 bg-primary-100 text-primary-700 rounded-full px-4 py-2 mb-4">
                <i class="bi bi-car-front"></i>
                <span class="text-sm font-semibold">Notre Flotte</span>
            </div>
            <h2 class="text-4xl lg:text-5xl font-bold text-slate-900 mb-4">Nos véhicules disponibles</h2>
            <p class="text-slate-600 text-lg max-w-2xl mx-auto">
                Découvrez notre sélection de véhicules récents et bien entretenus, prêts à vous accompagner dans tous vos déplacements.
            </p>
        </div>
        
        {% if voitures|length > 0 %}
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 mb-12">
                {% for voiture in voitures %}
                    <div class="card card-hover group">
                        <div class="relative overflow-hidden">
                            {% if voiture.photoPrincipale %}
                                <img src="{{ voiture.photoPrincipale }}" 
                                     alt="{{ voiture.marque }} {{ voiture.modele }}" 
                                     class="w-full h-64 object-cover group-hover:scale-110 transition-transform duration-500">
                            {% else %}
                                <div class="w-full h-64 bg-gradient-to-br from-slate-200 to-slate-300 flex items-center justify-center">
                                    <i class="bi bi-car-front text-slate-400 text-6xl"></i>
                                </div>
                            {% endif %}
                            
                            <div class="absolute top-4 right-4">
                                {% if voiture.statut == 'disponible' %}
                                    <span class="badge badge-success">
                                        <i class="bi bi-check-circle mr-1"></i>Disponible
                                    </span>
                                {% elseif voiture.statut == 'louee' %}
                                    <span class="badge badge-warning">
                                        <i class="bi bi-clock mr-1"></i>Louée
                                    </span>
                                {% else %}
                                    <span class="badge badge-danger">
                                        <i class="bi bi-x-circle mr-1"></i>Maintenance
                                    </span>
                                {% endif %}
                            </div>
                        </div>
                        
                        <div class="p-6">
                            <h3 class="text-2xl font-bold text-slate-900 mb-3">{{ voiture.marque }} {{ voiture.modele }}</h3>
                            
                            <div class="flex flex-wrap gap-2 mb-4">
                                <span class="badge bg-slate-100 text-slate-700">
                                    <i class="bi bi-calendar3 mr-1"></i>{{ voiture.annee }}
                                </span>
                                {% if voiture.nombrePlaces %}
                                <span class="badge bg-slate-100 text-slate-700">
                                    <i class="bi bi-people-fill mr-1"></i>{{ voiture.nombrePlaces }} places
                                </span>
                                {% endif %}
                                {% if voiture.typeCarburant %}
                                <span class="badge bg-slate-100 text-slate-700">
                                    <i class="bi bi-fuel-pump mr-1"></i>{{ voiture.typeCarburant }}
                                </span>
                                {% endif %}
                            </div>
                            
                            {% if voiture.description %}
                                <p class="text-slate-600 text-sm mb-4 line-clamp-2">{{ voiture.description }}</p>
                            {% endif %}
                            
                            <div class="flex items-center justify-between mb-6 pb-4 border-b border-slate-200">
                                <div>
                                    <span class="text-3xl font-bold gradient-text">{{ voiture.prixParJour }}</span>
                                    <span class="text-slate-500 text-sm ml-1">MAD/jour</span>
                                </div>
                            </div>
                            
                            <div class="space-y-3">
                                <a href="{{ path('app_voiture_show', {'id': voiture.id}) }}" class="btn-outline-primary w-full text-center">
                                    <i class="bi bi-eye mr-2"></i>Voir détails
                                </a>
                                {% if voiture.statut == 'disponible' and app.user and 'ROLE_CLIENT' in app.user.roles %}
                                    <a href="{{ path('app_reservation_new_voiture', {'voitureId': voiture.id}) }}" class="btn-primary w-full text-center">
                                        <i class="bi bi-calendar-check mr-2"></i>Réserver maintenant
                                    </a>
                                {% endif %}
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
            
            <div class="text-center">
                <a href="{{ path('app_voiture_index') }}" class="btn-outline-primary inline-flex items-center space-x-2">
                    <span>Voir toutes les voitures</span>
                    <i class="bi bi-arrow-right"></i>
                </a>
            </div>
        {% else %}
            <div class="card max-w-2xl mx-auto text-center py-16">
                <div class="mb-6">
                    <div class="w-24 h-24 bg-slate-100 rounded-full flex items-center justify-center mx-auto">
                        <i class="bi bi-car-front text-slate-400 text-5xl"></i>
                    </div>
                </div>
                <h4 class="text-2xl font-bold text-slate-900 mb-2">Aucune voiture disponible pour le moment</h4>
                <p class="text-slate-600 mb-6">Revenez bientôt pour découvrir notre flotte de véhicules.</p>
            </div>
        {% endif %}
    </div>
</section>

<!-- Features Section -->
<section class="py-16 lg:py-24 bg-gradient-to-b from-white to-slate-50">
    <div class="container mx-auto px-4">
        <div class="text-center mb-12">
            <div class="inline-flex items-center space-x-2 bg-green-100 text-green-700 rounded-full px-4 py-2 mb-4">
                <i class="bi bi-shield-check"></i>
                <span class="text-sm font-semibold">Pourquoi nous choisir</span>
            </div>
            <h2 class="text-4xl lg:text-5xl font-bold text-slate-900 mb-4">Une expérience premium</h2>
            <p class="text-slate-600 text-lg max-w-2xl mx-auto">
                Nous nous engageons à vous offrir le meilleur service de location de voitures au Maroc.
            </p>
        </div>
        
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
            <div class="card text-center p-8 group hover:shadow-2xl transition-all duration-300">
                <div class="w-20 h-20 bg-gradient-to-br from-green-500 to-green-600 rounded-2xl flex items-center justify-center mx-auto mb-6 group-hover:scale-110 transition-transform duration-300 shadow-lg shadow-green-500/30">
                    <i class="bi bi-shield-check text-white text-3xl"></i>
                </div>
                <h4 class="text-xl font-bold text-slate-900 mb-3">Assurance incluse</h4>
                <p class="text-slate-600">Tous nos véhicules sont entièrement assurés pour votre tranquillité d'esprit. Roulez en toute sérénité.</p>
            </div>
            
            <div class="card text-center p-8 group hover:shadow-2xl transition-all duration-300">
                <div class="w-20 h-20 bg-gradient-to-br from-accent-500 to-accent-600 rounded-2xl flex items-center justify-center mx-auto mb-6 group-hover:scale-110 transition-transform duration-300 shadow-lg shadow-accent-500/30">
                    <i class="bi bi-clock-history text-white text-3xl"></i>
                </div>
                <h4 class="text-xl font-bold text-slate-900 mb-3">Disponibilité 24/7</h4>
                <p class="text-slate-600">Réservez et récupérez votre véhicule à tout moment. Notre équipe est là pour vous, jour et nuit.</p>
            </div>
            
            <div class="card text-center p-8 group hover:shadow-2xl transition-all duration-300">
                <div class="w-20 h-20 bg-gradient-to-br from-primary-500 to-primary-600 rounded-2xl flex items-center justify-center mx-auto mb-6 group-hover:scale-110 transition-transform duration-300 shadow-lg shadow-primary-500/30">
                    <i class="bi bi-star-fill text-white text-3xl"></i>
                </div>
                <h4 class="text-xl font-bold text-slate-900 mb-3">Véhicules de qualité</h4>
                <p class="text-slate-600">Une flotte moderne et bien entretenue pour votre confort et sécurité. Des véhicules récents et fiables.</p>
            </div>
        </div>
    </div>
</section>

<!-- CTA Section -->
{% if not app.user %}
<section class="py-16 lg:py-24 bg-gradient-to-br from-slate-900 via-primary-900 to-slate-900 text-white">
    <div class="container mx-auto px-4">
        <div class="max-w-3xl mx-auto text-center">
            <h2 class="text-4xl lg:text-5xl font-bold mb-6">Prêt à prendre la route ?</h2>
            <p class="text-xl text-white/80 mb-8 leading-relaxed">
                Créez votre compte gratuitement et réservez votre véhicule en quelques minutes. 
                Rejoignez des centaines de clients satisfaits.
            </p>
            <div class="flex flex-wrap gap-4 justify-center">
                <a href="{{ path('app_register') }}" class="btn-primary inline-flex items-center space-x-2">
                    <i class="bi bi-person-plus"></i>
                    <span>Créer un compte</span>
                </a>
                <a href="{{ path('app_login') }}" class="btn-outline-primary border-2 border-white text-white hover:bg-white hover:text-primary-600 inline-flex items-center space-x-2">
                    <i class="bi bi-box-arrow-in-right"></i>
                    <span>Se connecter</span>
                </a>
            </div>
        </div>
    </div>
</section>
{% endif %}
{% endblock %}
