<!DOCTYPE html>
<html lang="fr" class="h-full">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="MarokiCars - Votre partenaire de confiance pour la location de voitures au Maroc. Large choix de véhicules, prix compétitifs, service 24/7.">
    <meta name="keywords" content="location voiture, Maroc, voiture, MarokiCars, location, véhicule">
    <meta name="author" content="MarokiCars">
    <meta name="theme-color" content="#3b82f6">
    
    <title>{% block title %}MarokiCars - Location de voitures au Maroc{% endblock %}</title>
    
    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>🚗</text></svg>">
    
    {{ encore_entry_link_tags('app') }}
</head>
<body class="h-full flex flex-col">
    <!-- Navbar -->
    <nav class="navbar">
        <div class="container mx-auto px-4">
            <div class="flex items-center justify-between h-20">
                <!-- Logo -->
                <a href="{{ path('home') }}" class="flex items-center space-x-3 group">
                    <div class="w-10 h-10 bg-gradient-to-br from-primary-500 to-primary-700 rounded-xl flex items-center justify-center transform group-hover:scale-110 transition-transform duration-300 shadow-lg shadow-primary-500/30">
                        <i class="bi bi-car-front-fill text-white text-xl"></i>
                    </div>
                    <span class="text-2xl font-bold text-white">MarokiCars</span>
                </a>

                <!-- Desktop Navigation -->
                <div class="hidden lg:flex items-center space-x-1">
                    <a href="{{ path('home') }}" class="px-4 py-2 text-white/90 hover:text-white hover:bg-white/10 rounded-lg transition-all duration-300 flex items-center space-x-2">
                        <i class="bi bi-house-door-fill"></i>
                        <span>Accueil</span>
                    </a>
                    <a href="{{ path('app_voiture_index') }}" class="px-4 py-2 text-white/90 hover:text-white hover:bg-white/10 rounded-lg transition-all duration-300 flex items-center space-x-2">
                        <i class="bi bi-car-front"></i>
                        <span>Voitures</span>
                    </a>
                    {% if app.user %}
                        {% if 'ROLE_CLIENT' in app.user.roles %}
                            <a href="{{ path('app_reservation_index') }}" class="px-4 py-2 text-white/90 hover:text-white hover:bg-white/10 rounded-lg transition-all duration-300 flex items-center space-x-2">
                                <i class="bi bi-calendar-check-fill"></i>
                                <span>Mes réservations</span>
                            </a>
                        {% endif %}
                        {% if 'ROLE_ADMIN' in app.user.roles %}
                            <a href="{{ path('app_voiture_new') }}" class="px-4 py-2 text-white/90 hover:text-white hover:bg-white/10 rounded-lg transition-all duration-300 flex items-center space-x-2">
                                <i class="bi bi-plus-circle-fill"></i>
                                <span>Ajouter une voiture</span>
                            </a>
                        {% endif %}
                    {% endif %}
                </div>

                <!-- User Menu -->
                <div class="hidden lg:flex items-center space-x-4">
                    {% if app.user %}
                        <div class="relative group">
                            <button class="flex items-center space-x-2 px-4 py-2 text-white/90 hover:text-white hover:bg-white/10 rounded-lg transition-all duration-300">
                                <i class="bi bi-person-circle text-2xl"></i>
                                <span class="font-medium">{{ app.user.nom }}</span>
                                <i class="bi bi-chevron-down text-sm"></i>
                            </button>
                            <div class="absolute right-0 mt-2 w-48 bg-white rounded-xl shadow-2xl opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-300 z-50 border border-slate-200">
                                <div class="py-2">
                                    {% if 'ROLE_ADMIN' in app.user.roles %}
                                        <a href="{{ path('app_admin_index') }}" class="block px-4 py-2 text-slate-700 hover:bg-slate-50 transition-colors flex items-center space-x-2">
                                            <i class="bi bi-gear-fill text-primary-600"></i>
                                            <span>Administration</span>
                                        </a>
                                        <div class="border-t border-slate-200 my-1"></div>
                                    {% endif %}
                                    <a href="{{ path('app_logout') }}" class="block px-4 py-2 text-slate-700 hover:bg-slate-50 transition-colors flex items-center space-x-2">
                                        <i class="bi bi-box-arrow-right text-red-500"></i>
                                        <span>Déconnexion</span>
                                    </a>
                                </div>
                            </div>
                        </div>
                    {% else %}
                        <a href="{{ path('app_login') }}" class="px-4 py-2 text-white/90 hover:text-white hover:bg-white/10 rounded-lg transition-all duration-300">
                            Connexion
                        </a>
                        <a href="{{ path('app_register') }}" class="btn-primary">
                            <i class="bi bi-person-plus-fill"></i>
                            Inscription
                        </a>
                    {% endif %}
                </div>

                <!-- Mobile Menu Button -->
                <button id="mobile-menu-button" class="lg:hidden text-white p-2 hover:bg-white/10 rounded-lg transition-colors">
                    <i class="bi bi-list text-2xl"></i>
                </button>
            </div>

            <!-- Mobile Navigation -->
            <div id="mobile-menu" class="hidden lg:hidden pb-4 space-y-2">
                <a href="{{ path('home') }}" class="block px-4 py-2 text-white/90 hover:text-white hover:bg-white/10 rounded-lg transition-all">
                    <i class="bi bi-house-door-fill mr-2"></i>Accueil
                </a>
                <a href="{{ path('app_voiture_index') }}" class="block px-4 py-2 text-white/90 hover:text-white hover:bg-white/10 rounded-lg transition-all">
                    <i class="bi bi-car-front mr-2"></i>Voitures
                </a>
                {% if app.user %}
                    {% if 'ROLE_CLIENT' in app.user.roles %}
                        <a href="{{ path('app_reservation_index') }}" class="block px-4 py-2 text-white/90 hover:text-white hover:bg-white/10 rounded-lg transition-all">
                            <i class="bi bi-calendar-check-fill mr-2"></i>Mes réservations
                        </a>
                    {% endif %}
                    {% if 'ROLE_ADMIN' in app.user.roles %}
                        <a href="{{ path('app_voiture_new') }}" class="block px-4 py-2 text-white/90 hover:text-white hover:bg-white/10 rounded-lg transition-all">
                            <i class="bi bi-plus-circle-fill mr-2"></i>Ajouter une voiture
                        </a>
                    {% endif %}
                    <div class="border-t border-white/20 my-2"></div>
                    {% if 'ROLE_ADMIN' in app.user.roles %}
                        <a href="{{ path('app_admin_index') }}" class="block px-4 py-2 text-white/90 hover:text-white hover:bg-white/10 rounded-lg transition-all">
                            <i class="bi bi-gear-fill mr-2"></i>Administration
                        </a>
                    {% endif %}
                    <a href="{{ path('app_logout') }}" class="block px-4 py-2 text-white/90 hover:text-white hover:bg-white/10 rounded-lg transition-all">
                        <i class="bi bi-box-arrow-right mr-2"></i>Déconnexion
                    </a>
                {% else %}
                    <div class="pt-2 space-y-2">
                        <a href="{{ path('app_login') }}" class="block px-4 py-2 text-white/90 hover:text-white hover:bg-white/10 rounded-lg transition-all text-center">
                            Connexion
                        </a>
                        <a href="{{ path('app_register') }}" class="block px-4 py-2 bg-primary-600 text-white rounded-lg text-center font-semibold hover:bg-primary-700 transition-all">
                            Inscription
                        </a>
                    </div>
                {% endif %}
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="flex-1">
        <!-- Flash Messages -->
        <div class="container mx-auto px-4 pt-4 space-y-3">
            {% for message in app.flashes('success') %}
                <div class="alert alert-success flex items-center justify-between animate-slide-up">
                    <div class="flex items-center space-x-2">
                        <i class="bi bi-check-circle-fill"></i>
                        <span>{{ message }}</span>
                    </div>
                    <button type="button" onclick="this.parentElement.remove()" class="text-green-800 hover:text-green-900">
                        <i class="bi bi-x-lg"></i>
                    </button>
                </div>
            {% endfor %}
            {% for message in app.flashes('error') %}
                <div class="alert alert-danger flex items-center justify-between animate-slide-up">
                    <div class="flex items-center space-x-2">
                        <i class="bi bi-exclamation-triangle-fill"></i>
                        <span>{{ message }}</span>
                    </div>
                    <button type="button" onclick="this.parentElement.remove()" class="text-red-800 hover:text-red-900">
                        <i class="bi bi-x-lg"></i>
                    </button>
                </div>
            {% endfor %}
            {% for message in app.flashes('warning') %}
                <div class="alert alert-warning flex items-center justify-between animate-slide-up">
                    <div class="flex items-center space-x-2">
                        <i class="bi bi-exclamation-circle-fill"></i>
                        <span>{{ message }}</span>
                    </div>
                    <button type="button" onclick="this.parentElement.remove()" class="text-amber-800 hover:text-amber-900">
                        <i class="bi bi-x-lg"></i>
                    </button>
                </div>
            {% endfor %}
        </div>

        {% block body %}{% endblock %}
    </main>

    <!-- Footer -->
    <footer class="footer py-12 mt-16">
        <div class="container mx-auto px-4">
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8 mb-8">
                <!-- Brand -->
                <div>
                    <div class="flex items-center space-x-3 mb-4">
                        <div class="w-12 h-12 bg-gradient-to-br from-primary-500 to-primary-700 rounded-xl flex items-center justify-center shadow-lg shadow-primary-500/30">
                            <i class="bi bi-car-front-fill text-white text-2xl"></i>
                        </div>
                        <span class="text-2xl font-bold text-white">MarokiCars</span>
                    </div>
                    <p class="text-slate-300 text-sm leading-relaxed max-w-xs">
                        Votre partenaire de confiance pour la location de voitures au Maroc. 
                        Des véhicules de qualité, des prix compétitifs et un service exceptionnel.
                    </p>
                </div>

                <!-- Navigation -->
                <div>
                    <h6 class="text-white font-semibold mb-4 text-sm uppercase tracking-wider">Navigation</h6>
                    <ul class="space-y-2">
                        <li>
                            <a href="{{ path('home') }}" class="text-slate-300 hover:text-white transition-colors text-sm">
                                Accueil
                            </a>
                        </li>
                        <li>
                            <a href="{{ path('app_voiture_index') }}" class="text-slate-300 hover:text-white transition-colors text-sm">
                                Nos voitures
                            </a>
                        </li>
                    </ul>
                </div>

                <!-- Contact -->
                <div>
                    <h6 class="text-white font-semibold mb-4 text-sm uppercase tracking-wider">Contact</h6>
                    <ul class="space-y-2">
                        <li class="text-slate-300 text-sm flex items-center space-x-2">
                            <i class="bi bi-envelope"></i>
                            <span>contact@marokicars.ma</span>
                        </li>
                        <li class="text-slate-300 text-sm flex items-center space-x-2">
                            <i class="bi bi-telephone"></i>
                            <span>+212 5XX XXX XXX</span>
                        </li>
                    </ul>
                </div>

                <!-- Social Media -->
                <div>
                    <h6 class="text-white font-semibold mb-4 text-sm uppercase tracking-wider">Suivez-nous</h6>
                    <div class="flex space-x-4">
                        <a href="#" class="w-10 h-10 bg-white/10 hover:bg-white/20 rounded-lg flex items-center justify-center text-white transition-all duration-300 hover:scale-110">
                            <i class="bi bi-facebook"></i>
                        </a>
                        <a href="#" class="w-10 h-10 bg-white/10 hover:bg-white/20 rounded-lg flex items-center justify-center text-white transition-all duration-300 hover:scale-110">
                            <i class="bi bi-instagram"></i>
                        </a>
                        <a href="#" class="w-10 h-10 bg-white/10 hover:bg-white/20 rounded-lg flex items-center justify-center text-white transition-all duration-300 hover:scale-110">
                            <i class="bi bi-twitter-x"></i>
                        </a>
                    </div>
                </div>
            </div>

            <!-- Copyright -->
            <div class="border-t border-white/10 pt-8 flex flex-col md:flex-row justify-between items-center">
                <p class="text-slate-400 text-sm">
                    &copy; {{ "now"|date("Y") }} MarokiCars. Tous droits réservés.
                </p>
                <div class="flex space-x-6 mt-4 md:mt-0">
                    <a href="#" class="text-slate-400 hover:text-white text-sm transition-colors">Mentions légales</a>
                    <a href="#" class="text-slate-400 hover:text-white text-sm transition-colors">Politique de confidentialité</a>
                </div>
            </div>
        </div>
    </footer>

    {{ encore_entry_script_tags('app') }}
    
    <script>
        // Mobile menu toggle
        document.getElementById('mobile-menu-button')?.addEventListener('click', function() {
            const menu = document.getElementById('mobile-menu');
            menu.classList.toggle('hidden');
        });
    </script>
</body>
</html>
