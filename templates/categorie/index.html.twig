{% extends 'base.html.twig' %}

{% block title %}Catégories - MarokiCars | Location de voitures au Maroc{% endblock %}

{% block body %}
<!-- Hero Section -->
<section class="hero-section py-20 lg:py-32 relative">
    <div class="container mx-auto px-4 relative z-10">
        <div class="text-center">
            <div class="inline-flex items-center space-x-2 bg-white/10 backdrop-blur-sm border border-white/20 rounded-full px-4 py-2 mb-6">
                <i class="bi bi-tags-fill text-accent-400"></i>
                <span class="text-white text-sm font-medium">Choisissez par catégorie</span>
            </div>
            
            <h1 class="text-5xl lg:text-7xl font-bold text-white mb-6 leading-tight">
                Explorez nos<br>
                <span class="gradient-text-accent">catégories</span>
            </h1>
            
            <p class="text-xl text-white/90 mb-8 max-w-2xl mx-auto leading-relaxed">
                Trouvez le véhicule parfait selon vos besoins : familial, économique, confortable ou tout-terrain.
            </p>
        </div>
    </div>
</section>

<!-- Categories Section -->
<section class="py-16 lg:py-24 bg-white">
    <div class="container mx-auto px-4">
        <div class="text-center mb-12">
            <h2 class="text-4xl lg:text-5xl font-bold text-slate-900 mb-4">Nos catégories de véhicules</h2>
            <p class="text-slate-600 text-lg max-w-2xl mx-auto">
                Chaque catégorie est adaptée à des besoins spécifiques pour garantir votre satisfaction.
            </p>
        </div>
        
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
            {% for categorie in categories %}
                <div class="card card-hover group cursor-pointer" onclick="window.location.href='{{ path('app_voiture_index', {'categorie': categorie.id}) }}'">
                    <div class="relative overflow-hidden">
                        <div class="h-48 bg-gradient-to-br {% if categorie.nom == 'Famille' %}from-blue-500 to-blue-600{% elseif categorie.nom == 'Confort' %}from-purple-500 to-purple-600{% elseif categorie.nom == 'Économique' %}from-green-500 to-green-600{% else %}from-orange-500 to-orange-600{% endif %} flex items-center justify-center">
                            <i class="bi {% if categorie.nom == 'Famille' %}bi-people-fill{% elseif categorie.nom == 'Confort' %}bi-star-fill{% elseif categorie.nom == 'Économique' %}bi-cash-stack{% else %}bi-truck{% endif %} text-white text-6xl"></i>
                        </div>
                        
                        <div class="absolute top-4 right-4">
                            <span class="badge bg-white/90 text-slate-800">
                                {{ categorie.voitures.count }} véhicule{{ categorie.voitures.count > 1 ? 's' : '' }}
                            </span>
                        </div>
                    </div>
                    
                    <div class="p-6">
                        <h3 class="text-2xl font-bold text-slate-900 mb-3">{{ categorie.nom }}</h3>
                        
                        {% if categorie.description %}
                            <p class="text-slate-600 text-sm mb-4 line-clamp-2">{{ categorie.description }}</p>
                        {% endif %}
                        
                        <div class="flex items-center justify-between">
                            <span class="text-sm text-slate-500">
                                <i class="bi bi-car-front mr-1"></i>
                                Voir les véhicules
                            </span>
                            <i class="bi bi-arrow-right text-primary-600 group-hover:translate-x-1 transition-transform"></i>
                        </div>
                    </div>
                </div>
            {% else %}
                <div class="col-12">
                    <div class="card max-w-2xl mx-auto text-center py-16">
                        <div class="mb-6">
                            <div class="w-24 h-24 bg-slate-100 rounded-full flex items-center justify-center mx-auto">
                                <i class="bi bi-tags text-slate-400 text-5xl"></i>
                            </div>
                        </div>
                        <h4 class="text-2xl font-bold text-slate-900 mb-2">Aucune catégorie disponible</h4>
                        <p class="text-slate-600 mb-6">Revenez bientôt pour découvrir nos catégories de véhicules.</p>
                    </div>
                </div>
            {% endfor %}
        </div>
        
        <div class="text-center mt-12">
            <a href="{{ path('app_voiture_index') }}" class="btn-outline-primary inline-flex items-center space-x-2">
                <span>Voir toutes les voitures</span>
                <i class="bi bi-arrow-right"></i>
            </a>
        </div>
    </div>
</section>

<!-- Features Section -->
<section class="py-16 lg:py-24 bg-gradient-to-b from-white to-slate-50">
    <div class="container mx-auto px-4">
        <div class="text-center mb-12">
            <h2 class="text-4xl lg:text-5xl font-bold text-slate-900 mb-4">Pourquoi choisir par catégorie ?</h2>
            <p class="text-slate-600 text-lg max-w-2xl mx-auto">
                Notre système de catégories vous aide à trouver rapidement le véhicule parfait pour vos besoins.
            </p>
        </div>
        
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
            <div class="text-center">
                <div class="w-20 h-20 bg-blue-100 rounded-2xl flex items-center justify-center mx-auto mb-6">
                    <i class="bi bi-people-fill text-blue-600 text-3xl"></i>
                </div>
                <h4 class="text-xl font-bold text-slate-900 mb-3">Famille</h4>
                <p class="text-slate-600">Espace optimal pour les voyages en famille avec enfants et bagages.</p>
            </div>
            
            <div class="text-center">
                <div class="w-20 h-20 bg-purple-100 rounded-2xl flex items-center justify-center mx-auto mb-6">
                    <i class="bi bi-star-fill text-purple-600 text-3xl"></i>
                </div>
                <h4 class="text-xl font-bold text-slate-900 mb-3">Confort</h4>
                <p class="text-slate-600">Équipements premium et confort supérieur pour vos déplacements.</p>
            </div>
            
            <div class="text-center">
                <div class="w-20 h-20 bg-green-100 rounded-2xl flex items-center justify-center mx-auto mb-6">
                    <i class="bi bi-cash-stack text-green-600 text-3xl"></i>
                </div>
                <h4 class="text-xl font-bold text-slate-900 mb-3">Économique</h4>
                <p class="text-slate-600">Consommation réduite et tarifs avantageux pour petits budgets.</p>
            </div>
            
            <div class="text-center">
                <div class="w-20 h-20 bg-orange-100 rounded-2xl flex items-center justify-center mx-auto mb-6">
                    <i class="bi bi-truck text-orange-600 text-3xl"></i>
                </div>
                <h4 class="text-xl font-bold text-slate-900 mb-3">4x4</h4>
                <p class="text-slate-600">Puissance et robustesse pour les terrains difficiles et aventures.</p>
            </div>
        </div>
    </div>
</section>
{% endblock %}
