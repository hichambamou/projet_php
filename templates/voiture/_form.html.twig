{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    <div class="row">
        <div class="col-md-6 mb-3">
            {{ form_row(form.marque) }}
        </div>
        <div class="col-md-6 mb-3">
            {{ form_row(form.modele) }}
        </div>
    </div>
    <div class="row">
        <div class="col-md-4 mb-3">
            {{ form_row(form.annee) }}
        </div>
        <div class="col-md-4 mb-3">
            {{ form_row(form.prixParJour) }}
        </div>
        <div class="col-md-4 mb-3">
            {{ form_row(form.statut) }}
        </div>
    </div>
    <div class="row">
        <div class="col-md-6 mb-3">
            {{ form_row(form.nombrePlaces) }}
        </div>
        <div class="col-md-6 mb-3">
            {{ form_row(form.typeCarburant) }}
        </div>
    </div>
    <div class="mb-3">
        {{ form_row(form.photoPrincipale) }}
    </div>
    <div class="mb-3">
        {{ form_row(form.description) }}
    </div>
    
    <div class="d-flex gap-2 mt-4">
        <button type="submit" class="btn btn-primary">
            <i class="bi bi-check-circle me-2"></i>{{ button_label|default('Enregistrer') }}
        </button>
        <a href="{{ path('app_voiture_index') }}" class="btn btn-outline-secondary">
            <i class="bi bi-x-circle me-2"></i>Annuler
        </a>
    </div>
{{ form_end(form) }}
