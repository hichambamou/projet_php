{% extends 'base.html.twig' %}

{% block title %}Edit PhotoVoiture{% endblock %}

{% block body %}
    <h1>Edit PhotoVoiture</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">Update</button>
    {{ form_end(form) }}

    <a href="{{ path('app_photo_voiture_index') }}">back to list</a>

    {{ include('photo_voiture/_delete_form.html.twig') }}
{% endblock %}
